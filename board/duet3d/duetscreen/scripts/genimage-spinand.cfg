flash nand-128M {
	pebsize = 128K
	numpebs = 1024
	minimum-io-unit-size = 2048
}

image duetscreen-spinand.img {
	flash {}
	flashtype = "nand-128M"

	partition BOOT {
		image = "boot-bundle.img"
		size = 1152K
	}

	partition UBI {
		image = "rootfs.ubi"
		offset = 1152K
	}
}

